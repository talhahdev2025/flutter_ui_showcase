import 'package:flutter/widgets.dart';
import 'app_spacing.dart';

final class AppInsets {
  AppInsets._();

static const EdgeInsets allXXs =
      EdgeInsets.all(AppSpacing.xxs);
  static const EdgeInsets allXs =
      EdgeInsets.all(AppSpacing.xs);

  static const EdgeInsets allSm =
      EdgeInsets.all(AppSpacing.sm);

  static const EdgeInsets allMd =
      EdgeInsets.all(AppSpacing.md);

  static const EdgeInsets allLg =
      EdgeInsets.all(AppSpacing.lg);

}